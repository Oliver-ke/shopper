import React from 'react';
import { SpinnerContainer, Spinner as SpinnerBlock } from './spinnerStyles';

const Spinner = () => {
	return (
		<SpinnerContainer>
			<SpinnerBlock />
		</SpinnerContainer>
	);
};

export default Spinner;
